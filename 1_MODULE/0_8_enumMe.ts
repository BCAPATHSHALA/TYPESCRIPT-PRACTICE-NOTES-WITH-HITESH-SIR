// Example 1: role types for an application
enum AppRole {
  USER,
  ADMIN,
}

const appRole = AppRole.ADMIN;

// Example 2: seat types of train
enum SeatChoice {
  AISLE = "aisle",
  MIDDLE = 3,
  WINDOW,
  FOURTH,
}

const hcSeat = SeatChoice.AISLE;
